# Бинарники Tailwind
tailwindcss.exe
tailwindcss-windows-x64.exe

# Node модули
node_modules/

# Файлы сборки Jekyll
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata

# Зависимости Ruby
# Gemfile.lock — НЕ игнорируем, нужен для воспроизводимых деплоев на GitHub Actions

# Локальный конфиг bundle (пути к локальным темам, машинно-специфичный)
.bundle/config
